{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.17.1",
    "npm": "^6.4.1",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.0",
    "antd": "^3.10.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "mkdir -p target/classes/public && react-scripts build && cp -r build/* target/classes/public",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "proxy": "http://localhost:8080/"
}
